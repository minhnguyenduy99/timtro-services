{
  "name": "timtro-mcp",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/timtro-mcp/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/dist"],
      "options": {
        "command": "tsc --watch",
        "cwd": "{projectRoot}"
      },
      "continuous": true
    },
    "serve": {
      "executor": "nx:run-commands",
      "dependsOn": ["build"],
      "options": {
        "command": "node dist/apps/timtro-mcp/server.js",
        "cwd": "{projectRoot}"
      }
    },
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsx apps/timtro-mcp/src/server.ts",
        "cwd": "{workspaceRoot}"
      }
    },
    "smoke": {
      "executor": "nx:run-commands",
      "dependsOn": ["build"],
      "options": {
        "command": "node scripts/smoke-call.mjs",
        "cwd": "{workspaceRoot}"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "dependsOn": ["build"],
      "options": {
        "command": "node scripts/smoke-call.mjs",
        "cwd": "{workspaceRoot}"
      }
    },
    "inspector": {
      "executor": "nx:run-commands",
      "dependsOn": ["build"],
      "options": {
        "command": "mcp-inspector node {projectRoot}/dist/server.js",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
